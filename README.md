Server should already be up,

Tech stack:
Django and REST api
sqlite db
python general purpose language
scikit-learn for ML

## Usage
1. start the server with ```python manage.py startserver```

I highly reccommend editing while in the terminal using tmux, but you can also edit with vscode using this:
```code --remote gfg /home/liam/gfgbackend```

